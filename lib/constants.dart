import 'package:flutter/material.dart';

// color constants
Color darkBg = const HSLColor.fromAHSL(1, 240, 0.03, 0.12).toColor();
Color aGreen = const HSLColor.fromAHSL(1, 119, 0.98, 0.40).toColor();
Color btnGreen = const HSLColor.fromAHSL(1, 119, 0.98, 0.30).toColor();
Color greyBg = const HSLColor.fromAHSL(0.19, 0, 0, 0.85).toColor();
Color white = Colors.white;
Color grey1 = Colors.white.withAlpha(150);
Color grey2 = Colors.white.withAlpha(180);

// sizing constants
double kDefaultPadding = 20.0;

// font sizes
double kSmallFontSize = 12.0;
double kDefaultFontSize = 14.0;
double kMediumFontSize = 16.0;
double kLargeFontSize = 24.0;

// font weights
FontWeight bold = FontWeight.bold;
FontWeight medium = FontWeight.w500;
FontWeight light = FontWeight.w100;
