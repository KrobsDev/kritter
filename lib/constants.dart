import 'package:flutter/material.dart';

// color constants
HSLColor darkBg = const HSLColor.fromAHSL(1, 240, 0.03, 0.12);
HSLColor aGreen = const HSLColor.fromAHSL(1, 119, 0.98, 0.30);
Color white = Colors.white;

// sizing constants
double kDefaultPadding = 20.0;

// fonts
double kDefaultFontSize = 14.0;
double kMediumFontSize = 16.0;
double kLargeFontSize = 24.0;
